<!DOCTYPE html>
<html>
    <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <%= stylesheet_link_tag 'application'%> 
    </head>
    <body>
        <main>

            <div class = "wrapper">
              <%= form_with model:@comment, url:comment_path(@num), method: :patch, local:true do |form| %>
                <div class = "form-group" style="width: 50rem">
                    <%= label_tag 'content' %>
                    <%= form.text_field :content, class:'form-control'%>
                </div>
                <%= form.submit "Edit" %>
              <%end%>

              <p></p>
                <%= form_with model:@comment, url:comment_path(@num), method: :delete, local:true do |form| %>
                  <%= form.submit "Trash !" %>
                <%end%>
              <p></p>

            </div>

            

        </main>
    </body>
</html>